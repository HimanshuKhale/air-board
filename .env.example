# Server-only. Never use a VITE_ prefix for secrets.
AIRBOARD_STT_PROVIDER=mock
AIRBOARD_AI_PORT=8787
AIRBOARD_DAILY_REQUEST_LIMIT=100
AIRBOARD_MOCK_TRANSCRIPT=AirBoard, create a flowchart with data collection, model training and deployment
# Required only when AIRBOARD_STT_PROVIDER=openai:
OPENAI_API_KEY=replace-with-your-server-side-key
